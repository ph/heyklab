apiVersion: cilium.io/v2alpha1
kind: CiliumBGPClusterConfig
metadata:
  name: bgp-cluster-config
spec:
  nodeSelector:
    matchLabels:
      node-role.kubernetes.io/control-plane: "true"
  bgpInstances:
    - name: "instance-65001"
      localASN: 65001
      peers:
        - name: "peer-65000"
          peerASN: 65000
          peerAddress: <router-ip>
          peerConfigRef:
            name: "cilium-peer-config"
---
apiVersion: cilium.io/v2alpha1
kind: CiliumBGPPeerConfig
metadata:
  name: cilium-peer-config
spec:
  families:
    - afi: ipv4
      safi: unicast
      advertisements:
        matchLabels:
          advertise: "bgp"
---
apiVersion: cilium.io/v2alpha1
kind: CiliumBGPAdvertisement
metadata:
  name: bgp-advertisements
  labels:
    advertise: "bgp"
spec:
  advertisements:
    - advertisementType: "PodCIDR"
    - advertisementType: "Service"
      service:
        addresses:
          - LoadBalancerIP
      selector:
        matchExpressions:
          - {key: cilium-lb-disabled, operator: NotIn, values: ['true']}
---
apiVersion: cilium.io/v2alpha1
kind: CiliumLoadBalancerIPPool
metadata:
  name: main-pool
  namespace: kube-system
spec:
  blocks:
    - cidr: 10.42.0.0/16
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   name: controlplane-lb
#   namespace: kube-system
# spec:
#   type: LoadBalancer
#   loadBalancerIP: <vip-for-kubernetes-api>
#   ports:
#     - port: 6443
#       targetPort: 6443
#       protocol: TCP
#       name: https
# ---
# apiVersion: discovery.k8s.io/v1
# kind: EndpointSlice
# metadata:
#   name: controlplane-lb
#   namespace: kube-system
#   labels:
#     kubernetes.io/service-name: controlplane-lb
# addressType: IPv4
# ports:
#   - port: 6443
#     protocol: TCP
#     name: https
# endpoints:
#   - addresses:
#       - 10.10.0.11
#       - 10.10.0.12
#       - 10.10.0.13
